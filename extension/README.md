# Xscape extension folder

Load this folder through `chrome://extensions` → **Load unpacked**.

Community Sync fetches a public GitHub JSON registry. The default is `NovasPlace/Xscape`, but the URL is editable in the popup.
